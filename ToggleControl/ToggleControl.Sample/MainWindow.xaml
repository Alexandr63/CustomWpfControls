<Window x:Class="ToggleControl.Samples.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:toggleControl="clr-namespace:ToggleControl;assembly=ToggleControl"
        mc:Ignorable="d"
        Title="MainWindow"
        Background="{DynamicResource BackgroundPrimary}"
        Height="500"
        Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToggleButton Grid.Row="1" Margin="10">
            Test toggle
        </ToggleButton>

        <ToggleButton Grid.Row="2" Margin="10" IsChecked="True">
            Test toggle
        </ToggleButton>

        <ToggleButton Grid.Row="3" Margin="10" IsEnabled="False">
            Test toggle
        </ToggleButton>

        <ToggleButton Grid.Row="4" Margin="10" IsChecked="True" IsEnabled="False">
            Test toggle
        </ToggleButton>
    </Grid>
</Window>
